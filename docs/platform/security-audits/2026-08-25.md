# Security audit — 2026-08-25

First full weekly infosec baseline per [security-audit-runbook.md](../security-audit-runbook.md).
**No production remediations in this pass** — findings only; promote HIGH into
[security-model.md](../security-model.md) backlog.

**Methods:** MCP `get_advisors` + `execute_sql` where authenticated; Management API
`GET /v1/projects/{ref}/advisors/security` for FM, Comms, Analytics/Stardom,
Pipeline v1, HRMS Sandbox. Advisors scan cost: **$0** (included in Pro).

## Summary

| Pri | System | Ref | ERROR | WARN | INFO | Truncate anon/auth | Anon DML | Notes |
|-----|--------|-----|-------|------|------|--------------------|----------|-------|
| P0 | SSO | `xgubaguglsnokjyudgvc` | 4 | 54 | 0 | clean | clean | 4× `security_definer_view` (= backlog **S2**); leaked-password WARN (= **S4**) |
| P0 | CDL | `ofzcokolkeejgqfjaszq` | 0 | 69 | 0 | clean | **30 tables** INSERT/UPDATE/DELETE | RLS on key tables; grants are hygiene drift (Wave 2 incomplete on DML) |
| P0 | CY Web Site | `yugymdytplmalumtmyct` | 0 | 34 | 0 | clean | clean | **HIGH:** anon can execute `update_user_role` / `remove_user_role` / `update_user_permission` SECURITY DEFINER RPCs |
| P1 | HRMS | `wltuhltnwhudgkkdsvsr` | 0 | 48 | 0 | **42 / 42** | **42 tables** + TRUNCATE | Wave 2F **not applied** / drifted; policies are `TO authenticated` so PostgREST DML blocked, but **TRUNCATE bypasses RLS** |
| P1 | FM | `retujkznogwplfrbniet` | 0 | 21 | 0 | n/a (API only) | n/a | Advisors WARN only (SECURITY DEFINER + search_path) |
| P1 | Datacore | `zcajghoohycimpubufsy` | 0 | 2 | 20 | clean | clean | INFO: `raw.*` RLS on, no policies (service_role ingest — expected); WARN: `rls_auto_enable` executable |
| P1 | Pipeline 2.0 | `kzvhqgpedapzqmwgikrw` | 0 | 7 | 0 | **9 / 9** | **9 tables** | Commission / opportunity tables; TRUNCATE risk |
| P1 | MSA CY | `rpoeezssicpzexarmwqq` | 0 | 96 | 3 | clean | 1 (`qobrix_user_cache`) | Large SECURITY DEFINER WARN surface |
| P1 | MSA Hungary | `ykgyzqnuqpwasxvesxva` | 0 | 94 | 3 | **63 / 63** | **63 tables** | Same class as HRMS — Wave 2F missing |
| P1 | Qobrix RLS | `ycbwgnihbrqammkgngum` | 0 | 2 | 0 | clean | clean | Best of sales CRM set |
| P1 | Comms | `ujowkipnqgtazmtdsnlm` | **5** | 18 | 11 | n/a | n/a | **HIGH:** RLS disabled on `gdpr_requests`, `audit_log`, `campaign_jobs`, `api_rate_counters`; SECURITY DEFINER view |
| P2 | ITSM | `irjrcskfcyierdbefrpk` | 0 | 18 | 1 | clean | clean | WARN: anon/auth SECURITY DEFINER helpers; `pg_net` in public |
| P2 | Atlas MLS | `wckwfbbqiupvallmhqbu` | 0 | 0 | 0 | clean | clean | Clean Advisors + Matrix SQL |
| P2 | Analytics+Stardom | `wjsafhylqujwbpqgjjlj` | 0 | 88 | 1 | n/a | n/a | Large SECURITY DEFINER WARN surface |
| P2 | Digital Employees | `mihslqjjclbrqelnjjpb` | — | — | — | — | — | **Skipped:** Management API 403 (PAT lacks access) |
| P2 | Client Connect | `jnmssbsjhsoyyxuxxzop` | — | — | — | — | — | **Skipped:** Management API 403 |
| P2 | Meeting Hub | `hefqrtlmxwvvtximsvsy` | — | — | — | — | — | **Skipped:** Management API 403 |
| P3 | Pipeline v1 | `mydojctcewxrbwjckuyz` | 3 | 44 | 0 | n/a | n/a | 3× SECURITY DEFINER views (`deals`, …) |
| P3 | CY SPA staging | `rlfxsieleseimylumhwc` | — | — | — | — | — | **Skipped:** Management API 403 |
| P3 | HRMS Sandbox | `xyvkeefqxabfcptiyoxm` | 0 | 48 | 0 | n/a | n/a | Same WARN profile as HRMS prod |

## New HIGH (proposed backlog IDs)

| ID | Project | Finding | Remediation |
|----|---------|---------|-------------|
| **S6** | HRMS, MSA Hungary, Pipeline 2.0 | `TRUNCATE` still granted to `anon` and/or `authenticated` on many `public` tables (HRMS 42, MSA-HU 63, Pipeline 9). RLS does not apply to `TRUNCATE`. | Re-apply Wave 2F pattern: `REVOKE TRUNCATE … FROM anon, authenticated` + `ALTER DEFAULT PRIVILEGES … REVOKE TRUNCATE`. Verify with runbook SQL. |
| **S7** | Comms `ujowkipnqgtazmtdsnlm` | Advisors ERROR: RLS **disabled** on `gdpr_requests`, `audit_log`, `campaign_jobs`, `api_rate_counters`. | Enable RLS; service-role-only or scoped policies; revoke anon grants. |
| **S8** | CY Web Site | Anon can `EXECUTE` SECURITY DEFINER RPCs that mutate roles/permissions (`update_user_role`, `remove_user_role`, `update_user_permission`, `handle_new_user_admin`, …). | `REVOKE EXECUTE ON FUNCTION … FROM PUBLIC, anon, authenticated` where not intentional; keep only intentional public RPCs (e.g. view counters) with input validation. |
| **S9** | CDL (+ partial MSA CY, Pipeline) | Anon still holds INSERT/UPDATE/DELETE on many tables (CDL ~30 incl. `contacts`, `transaction_management`). RLS is enabled and typically has no anon policy → PostgREST may deny, but grants violate defense-in-depth / Wave 2 intent. | `REVOKE` anon DML (and sensitive SELECT) project-wide; mirror Wave 2 migrations. |

## Known / accepted (no change this week)

| ID | Finding | Why |
|----|---------|-----|
| S1 | CDL multi-tenancy on listings | Open architecture item |
| S2 | SSO 4× SECURITY DEFINER views | Still ERROR in Advisors — unchanged |
| S4 | Leaked password protection off | Confirmed WARN on SSO (+ CY website Auth) |
| H2 / H3 | Mutable `search_path` / extensions in `public` | Widespread WARN; medium/low |
| SECURITY DEFINER helper RPCs (SSO/MSA/ITSM/…) | Many WARN 0028/0029 | Often intentional for RLS helpers; revoke `anon` execute where JWT is required — track as Wave 3 hygiene, not all HIGH |
| Datacore `raw.*` RLS + no policies | INFO | Intentional service_role photograph |
| Atlas MLS | Clean | Positive control |

## Matrix SQL detail

| Project | Result |
|---------|--------|
| SSO | Truncate 0; anon DML 0 |
| CDL | Truncate 0; anon DML on ~30 tables including PII (`contacts`) and commercial (`transaction_management`, `properties_published`, …) |
| CY Web | Truncate 0; anon DML 0; public SELECT/INSERT policies for marketing/leads (expected for public site) |
| HRMS | Truncate **anon 42 + authenticated 42**; policies on sensitive tables are `TO authenticated` only |
| Pipeline 2.0 | Truncate/DML on `activities`, `opportunity*`, `commission_*`, `broker_compensation`, `role_configurations`, … |
| MSA CY | Only `qobrix_user_cache` anon DML |
| MSA Hungary | Truncate/DML on **63** tables (contacts, contracts, ms_graph_tokens, …) |
| Qobrix RLS / ITSM / Atlas / Datacore | Truncate + anon DML clean |

## Non-Supabase (spot)

- Advisors themselves: **no daily schedule / no extra cost** — automation = our cron/Action later.
- PAT used for Management API cannot reach Digital Employees, Client Connect, Meeting Hub, CY SPA staging → expand org access or add MCP for next weekly run.
- `github-watcher` / Apache / Nyx / Databricks / Twilio: not deep-scanned this week; keep on weekly checklist.

## Recommended next actions (approval required)

1. **S6** migrations for HRMS + MSA Hungary + Pipeline 2.0 (TRUNCATE revoke + default privileges).
2. **S7** Comms RLS enablement on the four ERROR tables.
3. **S8** CY website `REVOKE EXECUTE` on admin SECURITY DEFINER RPCs from `anon`/`PUBLIC`.
4. **S9** CDL (and remaining) anon DML revoke pass.
5. Enable **S4** leaked-password protection on SSO (and review CY Auth).
6. Grant Management API / MCP access for skipped P2 projects before next weekly run.

## KB sources

- [security-audit-runbook.md](../security-audit-runbook.md)
- [security-model.md](../security-model.md) (Wave 2 / C7 / C8)
- Supabase [Database Advisors](https://supabase.com/docs/guides/database/database-advisors)
