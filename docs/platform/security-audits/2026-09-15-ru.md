# Аудит безопасности Sharp Matrix — 2026-09-15

Документ для коллег. Технический EN-отчёт: [2026-09-15.md](2026-09-15.md).  
Метод: [security-audit-runbook.md](../security-audit-runbook.md). Модель и бэклог: [security-model.md](../security-model.md).

**Что сделали в этот проход:** только сканирование и отчёт (Advisors + Matrix SQL + Auth/EF + github-watcher/TLS/htdocs).  
**Что не делали:** никаких миграций и правок в прод — обсуждаем на дейли, потом чиним.

База сравнения: аудит [2026-09-01](2026-09-01-ru.md). С тех пор HU Storefront вышел в прод (`sothebys-realty.hu`, cutover 2026-09-09).

---

## Краткий итог для дейли (5 минут)

| Категория | Суть |
|-----------|------|
| **Новая критичная находка** | **S18** на прод HU Storefront: таблица `conversion_sync_log` без RLS, при этом `anon` может SELECT/INSERT/UPDATE/DELETE. Это прямой эффект незакрытого **S16** (default privileges). **S19** на HRMS: 4 SECURITY DEFINER RPC без JWT могут писать в `employees` / оргструктуру. |
| **Что держится** | S14 (дыра записи в `properties`/`Member`/`Office`) — закрыта. S2/S7/S9/S13 — держатся. CDL/ITSM гранты чистые. **HRMS S6** (TRUNCATE/DML) держится. |
| **Что всё ещё открыто** | S4 (leaked-password), S6 (MSA HU TRUNCATE 66/66), S8 (CY admin RPC), S10, S11/S12, **S15/S16/S17** на HU, **S19** HRMS. |
| **Инфра** | github-watcher **15/15 OK** (добавили Comms `@prod`; ключ 1gh убран). TLS до ноября. `service_role` в бандлах htdocs — **0**. |
| **Недоступны** | Digital Employees, Client Connect, Meeting Hub, CY SPA staging — другая org Supabase (403). |

**Предлагаемый порядок после дейли:** S18 → S16 (default ACL) → **S19 HRMS revoke** → S15 → S8 → S6/S10 по мере выхода из теста.

---

## Журнал: исправлений сегодня нет

Только отчёт. Сырьё: `/home/bitnami/tmp/security-audit-2026-09-15/`.

---

## Дельта vs 2026-09-01

| Изменение | Деталь |
|-----------|--------|
| Улучшилось | CDL WARN 69→39; SSO/HRMS anon DML чистые и держатся; ITSM WARN ↓ |
| Ухудшилось | **HU ERROR 0→1** (S18); MSA CY WARN +40; Comms WARN +8; MSA HU TRUNCATE 64→66 |
| Новый объект | MSA Hungary sandbox `mstvgnekgeoftwajxscv` — первый замер, зеркало MSA HU |
| Контекст | HU Storefront = **прод** сайта Венгрии |

---

## Системы по приоритету (кратко)

### P0

1. **SSO** — 0 ERROR / 50 WARN. S4 off. S13 держится (anon DML = 0).
2. **CDL** — 0 ERROR / 39 WARN (−30). Anon DML = 0, anon SECDEF = 0. S1 архитектура без изменений.
3. **CY Website** — **S8 открыт**: 16 anon EXECUTE на admin RPC (`update_user_role`, `remove_user_role`, …). Не трогали.

### P1

4. **HRMS** — **постоянный weekly deep-dive.** S6 (таблицы) **держится**: truncate = 0, anon DML = 0, RLS везде, default ACL `anon=rxtm`. **S19 NEW:** 4 SECURITY DEFINER без JWT (`copy_ad_*`, `create_*_manager_relationships`) пишут в `employees` / `employee_managers` через `/rpc` с publishable-ключом. Sandbox 3.0 по-прежнему S10 — это не прод.
5. **FM** — S10 TRUNCATE 11/11.
6. **Datacore** — почти чистый; INFO по `raw.*` ожидаем.
7. **Pipeline 2.0** — **S12** anon DML на 9 CRM-таблицах.
8. **MSA CY** — **S11** ERROR на view `opportunity_field_mismatches`; WARN вырос до 158.
9. **MSA Hungary** — **S6** TRUNCATE **66/66**, anon DML 264; тестирование.
10. **Qobrix RLS** — эталон в CRM-группе.
11. **Comms** — S7 держится; ERROR на view `conversations_enriched`; S10 TRUNCATE 17/17.
12. **HU Storefront (ПРОД)** — см. блок ниже.

### P2 / P3

- Atlas / Task Manager HU / Performance Dashboard — чистые.
- Analytics / Vacations / lead-generator / Pipeline v1 / HRMS sandbox — S10.
- **MSA Hungary sandbox** — первый аудит: как прод MSA HU (66/66 TRUNCATE).

---

## HU Storefront — глубокий re-check после cutover

| ID | Статус | Что это значит |
|----|--------|----------------|
| **S14** | **ДЕРЖИТСЯ** | Аноним больше не пишет в `properties` / `Member` / `Office`. |
| **S15** | открыт | Политика `Anyone can update view count` позволяет UPDATE любой строки `shared_collections`. |
| **S16** | открыт | Default privileges всё ещё дают anon write на **новые** таблицы. |
| **S17** | открыт | Любой `authenticated` может INSERT в `audit_log`. |
| **S18 NEW** | **HIGH** | Новая таблица `conversion_sync_log`: RLS выключен + полный anon DML. Поля: inquiry/opportunity id, payload jsonb. Advisors ERROR. |

Публичные INSERT (заявки, newsletter, visitor_*, chat, collections) по-прежнему есть — это ожидаемая поверхность сайта; отдельно стоит пересмотреть DELETE/UPDATE у anon.

---

## Недоступные системы (вторая org)

| Система | Ref |
|---------|-----|
| Digital Employees | `mihslqjjclbrqelnjjpb` |
| Client Connect | `jnmssbsjhsoyyxuxxzop` |
| Meeting Hub | `hefqrtlmxwvvtximsvsy` |
| CY SPA staging | `rlfxsieleseimylumhwc` |

Рекомендация без изменений: расширить PAT / MCP до следующего weekly.

---

## Бэклог после прохода

| ID | Статус | Суть |
|----|--------|------|
| S4 | открыт | Leaked-password off (SSO + CY + HU) |
| S6 | частично | HRMS DONE; MSA Hungary 66/66 (+ sandbox) |
| S8 | открыт | CY admin RPC для anon |
| S10 | открыт | Wave 2F на оставшихся проектах |
| S11 | открыт | MSA CY SECURITY DEFINER view |
| S12 | открыт | Pipeline 2.0 anon DML |
| S15 | открыт | HU `shared_collections` wide UPDATE |
| S16 | открыт | HU default ACL — **причина S18** |
| S17 | открыт | HU forge `audit_log` |
| **S18** | **NEW** | HU `conversion_sync_log` RLS off + anon DML |
| **S19** | **NEW** | HRMS anon SECDEF mutators (оргструктура / AD copy) |
| ~~S13~~ / ~~S14~~ | DONE / HOLD | SSO hygiene; HU write hole |

---

## Non-Supabase

- **github-watcher:** 15/15 OK; Comms на `@prod`; leftover 1gh **удалён** из config.
- **TLS:** все сертификаты до ноября 2026 (раньше всех — Nyx, 3 ноября).
- **htdocs:** строк `service_role` в бандлах нет.

---

## Talking points на дейли

1. S18 на **прод** HU — чинить первым (RLS + revoke anon), это не «theoretical».
2. Без S16 (default privileges) S18 повторится на следующей Lovable-таблице.
3. S8 CY — миграция уже была в `proposed/` с 09-01; когда готовы закрыть.
4. MSA HU всё ещё в тесте → S6 откладываем, но truncate растёт (64→66).
5. Вторая org по-прежнему без Advisors — риск слепой зоны на Digital Employees / Client Connect / Meeting Hub.
