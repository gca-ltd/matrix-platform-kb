# Аудит безопасности Sharp Matrix — 2026-09-15

Документ для коллег. Технический EN-отчёт: [2026-09-15.md](2026-09-15.md).  
Метод: [security-audit-runbook.md](../security-audit-runbook.md). Модель и бэклог: [security-model.md](../security-model.md).

**Что сделали:** только сканирование (Advisors + Matrix SQL + Auth/EF + github-watcher / TLS / htdocs).  
**Что не делали:** миграций в прод нет — обсуждаем на дейли, потом чиним.

База: [2026-09-01](2026-09-01-ru.md). HU Storefront с 2026-09-09 — **прод** `sothebys-realty.hu`.

---

## Краткий итог для дейли

| Категория | Суть |
|-----------|------|
| **Новое HIGH** | **S18** прод HU: `conversion_sync_log` без RLS + anon DML (следствие S16). **S19** HRMS: 4 SECURITY DEFINER RPC без JWT пишут в `employees` / оргструктуру. |
| **Держится** | S14, S2, S7, S9, S13. CDL/ITSM гранты чистые. **HRMS S6** (таблицы) держится. |
| **Открыто** | S4, S6 (MSA HU 66/66), S8, S10, S11/S12, S15–S17, S19. |
| **Инфра** | Watcher 15/15 OK. TLS до ноября. `service_role` в htdocs = 0. |
| **Слепая зона** | Digital Employees, Client Connect, Meeting Hub, CY SPA staging — 403 (вторая org). |

**Порядок после дейли:** S18 → S16 → S19 → S15 → S8 → S6/S10 после теста.

Сырьё: `/home/bitnami/tmp/security-audit-2026-09-15/`.

---

## Системы по одной

Для каждой: назначение → цифры прохода → проблема → чем грозит → как решаем.

### 1. Matrix SSO (`xgubaguglsnokjyudgvc`) — P0

**Назначение:** единый вход, роли, тенанты, OAuth, JWT.  
**Цифры:** 0 ERROR / 50 WARN (−4 vs 09-01). TRUNCATE 0. Anon DML 0. Anon SECDEF EXECUTE 19. EF `verify_jwt=false`: 40 (конвенция SSO). Public buckets: `avatars`, `app-icons`, `tenant-branding`.  
**Проблема:** **S4** — `password_hibp_enabled=false`. S13 (anon DML на `sso_permission_load_failures`) **держится закрытым**. WARN в основном search_path + SECDEF helpers.  
**Чем грозит:** на SSO можно поставить пароль из известных утечек (HaveIBeenPwned). 19 helper-RPC для anon — класс как S8, но это проверки `has_permission` / `get_my_*`, не смена ролей.  
**Как решаем:** S4 — включить leaked-password в Dashboard Auth (меняет UX логина). Helper EXECUTE — hygiene revoke с `anon`, если SPA ходит только через EF. **Сегодня не трогаем.**

### 2. Matrix CDL (`ofzcokolkeejgqfjaszq`) — P0

**Назначение:** канонические листинги RESO / MLS pipeline.  
**Цифры:** 0 ERROR / 39 WARN (−30). TRUNCATE 0. Anon DML 0. Anon SECDEF EXECUTE **0** (Matrix SQL).  
**Проблема:** открытый **S1** (нет tenant-scope на `properties`). WARN — search_path / extensions.  
**Чем грозит:** S1 — архитектурный: второй тенант с отдельным MLS увидит чужой каталог, если не режем на EF. Табличный anon-write **закрыт** (S9 держится).  
**Как решаем:** S1 — продуктовое решение (tenant_id vs source_id allow-list). Гранты не трогаем.

### 3. CY Website (`yugymdytplmalumtmyct`) — P0

**Назначение:** публичный сайт Кипра.  
**Цифры:** 0 ERROR / 34 WARN. TRUNCATE 0. Anon DML 0. **16 anon SECDEF EXECUTE.** HIBP off.  
**Проблема:** **S8** — anon вызывает admin RPC: `update_user_role`, `remove_user_role`, `update_user_permission`, `handle_new_user_admin`, плюс `get_users_with_permissions` / `get_users_with_roles` / `can_manage_*`. Также S4.  
**Чем грозит:** с publishable-ключом сайта можно менять роли и права через `/rest/v1/rpc/…` без логина.  
**Как решаем:** `REVOKE EXECUTE … FROM PUBLIC, anon` (SQL уже в `proposed/s8_cy_website_revoke_admin_rpc.sql`). Оставить только намеренные public RPC (`increment_property_views` и т.п.). **Не трогаем, пока не согласуем на дейли.**

### 4. HRMS (`wltuhltnwhudgkkdsvsr`) — P1 · постоянный deep-dive

**Назначение:** кадры, отпуска, оргструктура, performance (интранет, PII). С этого прохода — **еженедельный deep-dive**, не только счётчики грантов.  
**Цифры:** 0 ERROR / 46 WARN. TRUNCATE **0**. Anon DML **0**. RLS на всех public-таблицах. Default ACL postgres: `anon=rxtm` (чтение, без write/TRUNCATE) — эталон vs HU. 18 anon SECDEF. 9 EF без JWT (sync/AD/reminders). Buckets: `avatars`, `social-media`.  
**Что держится (S6):** табличный write/TRUNCATE для anon закрыт 2026-09-01 и **подтверждён**. SELECT на `employees` / `vacations` / `performance_reviews` — политики `TO authenticated` + tenant/scope; leftover GRANT SELECT у anon **не читает PII**.  
**Проблема (S19 NEW):** 4 SECURITY DEFINER **без проверки JWT**: `copy_ad_azure_oid_to_sso_user_id`, `copy_ad_photos_to_employees`, `create_manager_relationships_from_display_name`, `create_dotted_manager_relationships_from_display_name`. Тела: `UPDATE employees` / `INSERT employee_managers` как owner. Ещё 5 — триггеры; 9 — identity-хелперы.  
**Чем грозит:** ключ anon есть в бандле `/hrms/` → любой, кто достучался до PostgREST, может переписать оргструктуру и SSO-привязки. Не утечка зарплат через SELECT, но порча HR-данных.  
**Как решаем:** `REVOKE EXECUTE FROM PUBLIC, anon` на 4 мутатора (+ hygiene на остальные 14). Вызов оставить в EF под `service_role`. SPA не должен звать эти RPC напрямую. **Не применяли сегодня.**  
**Не путать:** sandbox 3.0 (`xyvke…`) — отдельная система, там S10.

### 5. Matrix FM (`retujkznogwplfrbniet`) — P1

**Назначение:** финансы, submissions, allocations.  
**Цифры:** 0 ERROR / 21 WARN. **TRUNCATE 11/11.** Anon DML 44 на 11 таблиц (`financial_entries`, `entities`, `audit_log`, …). 10 anon SECDEF.  
**Проблема:** **S10** — Wave 2F не накатывали.  
**Чем грозит:** `TRUNCATE` обходит RLS: роль с грантом может опустошить финансовые таблицы. Anon DML опасен, если политика `USING(true)` (нужна проверка политик перед revoke).  
**Как решаем:** шаблон `proposed/s10_wave2f_template.sql` — сначала TRUNCATE, потом точечный REVOKE DML. **Отложено** (нет доказанного freeze).

### 6. Datacore (`zcajghoohycimpubufsy`) — P1

**Назначение:** Qobrix photograph / insights.  
**Цифры:** 0 ERROR / 6 WARN / 25 INFO. TRUNCATE 0. Anon DML 0. 1 SECDEF (`rls_auto_enable`).  
**Проблема:** INFO `rls_enabled_no_policy` на `raw.*` — ожидаемо (ingest `service_role`).  
**Чем грозит:** низкий: `public` — контракт агента без грантов anon.  
**Как решаем:** ничего. Не включать политики на `raw` «для галочки».

### 7. Pipeline 2.0 (`kzvhqgpedapzqmwgikrw`) — P1

**Назначение:** CRM (app DB).  
**Цифры:** 0 ERROR / 7 WARN. TRUNCATE 0 (S6 slice с 08-25 держится). **Anon DML 27 / 9 таблиц:** `opportunity`, `activities`, `opportunity_link`, `commission_*`, `cost_rate_card`, `deal_cost_event`, `broker_compensation`, `role_configurations`.  
**Проблема:** **S12**.  
**Чем грозит:** если на этих таблицах есть политика под `{public}`/`anon`, publishable-ключ CRM пишет сделки и комиссии. Даже с узкой политикой грант — лишняя поверхность.  
**Как решаем:** REVOKE anon DML по паттерну S9. **Отложено** (активная разработка).

### 8. MSA CY (`rpoeezssicpzexarmwqq`) — P1

**Назначение:** Sales Automation Cyprus (staging, живое тестирование).  
**Цифры:** **1 ERROR** / 158 WARN (+40). TRUNCATE 0. Anon DML 27 / 9 объектов. 67 anon SECDEF. 71 EF без JWT.  
**Проблема:** **S11** — view `opportunity_field_mismatches` SECURITY DEFINER (ERROR). WARN: 67 executable SECDEF + search_path.  
**Чем грозит:** view читает как owner, минуя RLS вызывающего — утечка рассинхрона полей. Большой WARN — шум теста, не всё exploitable.  
**Как решаем:** `ALTER VIEW … SET (security_invoker = true)` после freeze. SECDEF — отдельная волна, как S8. **Не трогаем — тест.**

### 9. MSA Hungary (`ykgyzqnuqpwasxvesxva`) — P1

**Назначение:** Sales Automation Hungary (staging, тест).  
**Цифры:** 0 ERROR / 101 WARN. **TRUNCATE 66/66** (было 64). Anon DML 264. 47 anon SECDEF.  
**Проблема:** **S6 открыт.**  
**Чем грозит:** TRUNCATE обходит RLS на всём CRM HU (leads, contacts, opportunities, contracts, …). Рост 64→66 = новые таблицы снова приходят с дефолтным `D`.  
**Как решаем:** Wave 2F после freeze + сразу default privileges, иначе снова вырастет. Sandbox — тем же SQL. **Не трогаем — тест.**

### 10. Qobrix RLS (`ycbwgnihbrqammkgngum`) — P1

**Назначение:** тонкий CRM над Qobrix.  
**Цифры:** 0 ERROR / 2 WARN. TRUNCATE 0. Anon DML 0. 1 SECDEF (`is_sso_admin_v2`).  
**Проблема:** нет существенной. Эталон в CRM-группе.  
**Чем грозит:** —  
**Как решаем:** ничего. Положительный контроль.

### 11. Matrix Comms (`ujowkipnqgtazmtdsnlm`) — P1

**Назначение:** мессенджеры / WhatsApp.  
**Цифры:** **1 ERROR** / 26 WARN (+8). TRUNCATE **17/17**. Anon DML 68. ERROR: view `conversations_enriched` SECURITY DEFINER.  
**Проблема:** **S7 держится** (RLS на ops-таблицах с 08-25). Остался view-ERROR (как S11) + **S10** TRUNCATE.  
**Чем грозит:** view может отдать чужие чаты как owner. TRUNCATE — опустошение `messages` / `conversations`.  
**Как решаем:** `security_invoker` на view; Wave 2F на TRUNCATE. **Не трогаем в этом проходе.**

### 12. HU Storefront (`bpaxqtxaysolzaeguwvg`) — P1 · ПРОД

**Назначение:** `sothebys-realty.hu` (cutover 2026-09-09). Публичный интернет.  
**Цифры:** **1 ERROR** / 36 WARN. TRUNCATE 0. Anon DML 30 / 10 таблиц (публичные формы). 16 SECDEF. 22 EF без JWT (публичный сайт; MCP/embed с bearer/scheduler). Site URL всё ещё `…/sso-staging-main`. Buckets: `agent-photos`, `property-images`, `property-videos`, `blog-images`.  
**Проблемы:**

| ID | Статус | Суть | Чем грозит | Как решаем |
|----|--------|------|------------|------------|
| S14 | **HOLD** | Нет anon write на `properties` / `Member` / `Office` | — | Не трогать |
| S15 | OPEN | `shared_collections` UPDATE USING(true) | Аноним переписывает любую шару, не только счётчик | RPC / column-policy + REVOKE UPDATE (нужен UI) |
| S16 | OPEN | default ACL `anon=arwdxtm` | Следующая Lovable-таблица = новая S14/S18 | `ALTER DEFAULT PRIVILEGES REVOKE` write с anon |
| S17 | OPEN | `authenticated` INSERT в `audit_log` | Любой залогиненный куёт аудит | Политика только `service_role` |
| **S18 NEW** | OPEN | `conversion_sync_log` RLS **off** + полный anon DML | Утечка/подмена inquiry, opportunity id, jsonb payload с ключа сайта | RLS + revoke anon, затем S16 |

Публичные INSERT (заявки, newsletter, visitor_*, chat, collections) — ожидаемая поверхность; отдельно пересмотреть DELETE/UPDATE у anon.  
**Как решаем первым:** S18+S16 на дейли, потом S15/S17.

### 13. ITSM (`irjrcskfcyierdbefrpk`) — P2

**Назначение:** тикеты.  
**Цифры:** 0 ERROR / 12 WARN (−6). TRUNCATE 0. Anon DML 0. Anon SECDEF **0** (revoke 09-01 держится). 16 EF без JWT (`microsoft-auth`, `vendor-logo` — намеренно).  
**Проблема:** нет табличной. WARN — search_path / authenticated SECDEF.  
**Чем грозит:** низкий.  
**Как решаем:** ничего обязательного.

### 14. Atlas MLS (`wckwfbbqiupvallmhqbu`) — P2

**Назначение:** CDL admin SPA.  
**Цифры:** **0 ERROR / 0 WARN.** TRUNCATE 0. Anon DML 0. SECDEF 0.  
**Проблема:** нет. Эталон платформы.  
**Как решаем:** ничего.

### 15. Analytics + Stardom (`wjsafhylqujwbpqgjjlj`) — P2

**Назначение:** общая app DB аналитики и Stardom.  
**Цифры:** 0 ERROR / 88 WARN. **TRUNCATE 50/50.** Anon DML 200 / 50 таблиц. 32 anon SECDEF (включая `assign_lead`, `create_lead`, …).  
**Проблема:** **S10** + большой SECDEF.  
**Чем грозит:** TRUNCATE на CRM-подобных таблицах; RPC создания лидов с anon-ключа, если политики не режут.  
**Как решаем:** Wave 2F, затем точечный revoke EXECUTE. **Отложено.**

### 16. Career Connect (`zsjwbspjlpaxfadjeymd`) — P2

**Назначение:** вакансии / отклики.  
**Цифры:** 0 ERROR / 9 WARN. TRUNCATE 0 (фикс 09-01 держится). Anon DML 1: INSERT `career_events` (намеренно). 4 SECDEF, в т.ч. `handle_new_user_admin`.  
**Проблема:** `handle_new_user_admin` для anon — тот же класс, что S8, меньший blast radius.  
**Чем грозит:** регистрация/апрув админа без сессии, если функция не проверяет JWT.  
**Как решаем:** проверить тело; при отсутствии auth — REVOKE с anon. INSERT `career_events` оставить. **Не трогали.**

### 17. Task Manager HU (`rwgfixcfgviaqonhhqev`) — P2

**Цифры:** 0 / 0 / 0. TRUNCATE 0. DML 0. SECDEF 0.  
**Проблема:** нет.  
**Как решаем:** ничего.

### 18. Performance Dashboard (`patgnfubqbyaiapviksu`) — P2

**Цифры:** 0 / 0 / 0. Чисто.  
**Как решаем:** ничего.

### 19. Vacations Management (`kposeyhvgusosuzjjrdv`) — P2

**Назначение:** отпуска (отдельный проект от HRMS).  
**Цифры:** 0 ERROR / 0 WARN / 17 INFO. **TRUNCATE 17/17.** Anon DML 68 на 17 таблиц (`employees`, `leave_requests`, `payroll`-adjacent entitlements, …). INFO: RLS on, no policy.  
**Проблема:** **S10** + таблицы с RLS без политик (для anon это «закрыто», для тех, у кого GRANT — зависит от роли).  
**Чем грозит:** TRUNCATE опустошает отпуска; если authenticated ходит без политики — 0 rows, не утечка.  
**Как решаем:** Wave 2F. Политики — отдельно, если приложение реально пишет как authenticated.

### 20. matrix-lead-generator (`ddairradcxczsvwntwmw`) — P2

**Цифры:** 0 / 0 / 0. **TRUNCATE 7/7.** Anon DML 7 (`leads`, `contacts`, `conversations`, …).  
**Проблема:** **S10**.  
**Чем грозит:** TRUNCATE/DML на лидах.  
**Как решаем:** Wave 2F + понять, нужен ли публичный INSERT.

### 21. Pipeline v1 (`mydojctcewxrbwjckuyz`) — P3 legacy

**Назначение:** старый CRM; legacy интеграций.  
**Цифры:** **3 ERROR** / 44 WARN. TRUNCATE 50/50. Anon DML 200. 3 SECURITY DEFINER views.  
**Проблема:** S10 + views как S11.  
**Чем грозит:** тот же класс, но система на закате.  
**Как решаем:** не вкладываться, пока живёт; при декомиссии — выключить Data API / удалить проект.

### 22. HRMS Sandbox 3.0 (`xyvkeefqxabfcptiyoxm`) — P3

**Назначение:** клон прода HRMS (prod-like PII).  
**Цифры:** 0 ERROR / 48 WARN. **TRUNCATE 42/42.** Anon DML 168 (как прод *до* фикса 09-01). Те же 4 мутатора, что S19.  
**Проблема:** **S10** + зеркало S19.  
**Чем грозит:** утечка/порча клона с реальными кадрами, если sandbox торчит в интернет.  
**Как решаем:** либо тот же Wave 2F + S19, что на прод, либо убрать PII / закрыть Data API. **Не замена прода** в аудите.

### 23. MSA Hungary sandbox (`mstvgnekgeoftwajxscv`) — P3 · первый замер

**Назначение:** Lovable-клон MSA HU (SR000537), prod-like.  
**Цифры:** как прод MSA HU: 0 ERROR / 102 WARN. TRUNCATE **66/66**. Anon DML 264.  
**Проблема:** то же **S6**, что у живого MSA HU.  
**Чем грозит:** клон с боевыми паттернами грантов.  
**Как решаем:** тот же Wave 2F, когда закроем прод MSA HU. Не копировать `STOREFRONT_SERVICE_ROLE_KEY` на sandbox (уже в app-catalog).

---

## Недоступные (вторая org) — 403

| Система | Ref | Чем грозит | Как решаем |
|---------|-----|------------|------------|
| Digital Employees | `mihslqjjclbrqelnjjpb` | Нет Advisors/SQL — слепая зона на AI Agents + converse API | Расширить PAT / MCP (`user-lovable-mde` сейчас needsAuth) |
| Client Connect | `jnmssbsjhsoyyxuxxzop` | Контакты без еженедельного скана | Тот же доступ ко второй org |
| Meeting Hub | `hefqrtlmxwvvtximsvsy` | Встречи без скана | Тот же |
| CY SPA staging | `rlfxsieleseimylumhwc` | Staging; кандидат на миграцию/удаление | Доступ или decommission |

---

## Вне скоупа

Templates, CDL Studio, Lovable Source, cleanup `tiuansahlsgautkjsajk` / `iooyncgcumgecznfpnsk` / `hxbzyfadhwzlvfjgqase`.  
**Заметка:** KB `references/index.md` всё ещё зовёт HU `bpax…` «staging clone» — drift после cutover, чинить отдельно.

---

## Non-Supabase

| Проверка | Результат | Чем грозит | Как решаем |
|----------|-----------|------------|------------|
| github-watcher | **15/15 OK**; Comms `@prod` добавлен; ключ 1gh **нет** в config | Раньше named-but-missing secret = неподписанный деплой — сейчас нет | Держать секреты nonempty |
| TLS | intranet 20.11; Nyx **03.11**; MLS 14.11; HU 13.11 | Первым истекает Nyx (~7 недель) | certbot.timer; следить за Nyx |
| htdocs | `service_role` = **0** в 14 бандлах | JWT-shaped строки 2–8 на приложение — клиентский JWT / publishable, не service_role | Повторять grep weekly |
| Nyx / Databricks / Twilio | Не глубоко | Warehouse stay-awake — деньги, не дыра | Не будить warehouse «для проверки» |

---

## Бэклог

| ID | Где | Статус | Как закрываем |
|----|-----|--------|---------------|
| S4 | SSO, CY, HU Auth | открыт | Dashboard Auth → leaked-password |
| S6 | MSA HU + sandbox | открыт (HRMS прод DONE) | Wave 2F после freeze |
| S8 | CY Website | открыт | REVOKE admin RPC (`proposed/` готов) |
| S10 | FM, Comms, Analytics, Vacations, lead-gen, Pipeline v1, HRMS sandbox | открыт | `s10_wave2f_template.sql` |
| S11 | MSA CY | открыт | `security_invoker` на view |
| S12 | Pipeline 2.0 | открыт | REVOKE anon DML |
| S15–S17 | HU | открыт | см. систему 12 |
| **S18** | HU прод | **NEW** | RLS + revoke на `conversion_sync_log`, затем S16 |
| **S19** | HRMS прод | **NEW** | REVOKE EXECUTE на 4 мутатора |
| S1 | CDL | архитектура | tenant_id / allow-list |
| ~~S13~~ / ~~S14~~ | SSO / HU | HOLD | — |

---

## Talking points на дейли

1. S18 на **прод** HU — чинить первым.  
2. Без S16 дыра повторится на следующей таблице.  
3. S19 HRMS — revoke RPC; табличный S6 уже зелёный.  
4. S8 CY — SQL готов с 09-01.  
5. MSA HU truncate растёт (64→66), пока в тесте.  
6. Четыре системы второй org по-прежнему без скана.
